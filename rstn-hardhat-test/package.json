{
  "name": "rstn-hardhat-test",
  "version": "1.0.0",
  "private": true,
  "description": "Prueba de compatibilidad EVM de RSTN Network con Hardhat",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network rstn",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
